# Project-level overrides — these take priority over career/.env.example.
# Most keys (ANTHROPIC_API_KEY, GROQ_API_KEY, etc.) live in career/.env.
# Only add values here if this project needs different settings than the master.

# RAG pipeline settings
# CHROMA_PATH=./chroma_db
# COLLECTION_NAME=rag_corpus
# HYBRID_ALPHA=0.7         # weight for vector vs BM25 (0=BM25 only, 1=vector only)
# CHUNK_SIZE=256
# CHUNK_OVERLAP=32
