# Copy to .env and fill in values. All keys used by this project are listed below.

# RAG pipeline settings
# CHROMA_PATH=./chroma_db
# COLLECTION_NAME=rag_corpus
# HYBRID_ALPHA=0.7         # weight for vector vs BM25 (0=BM25 only, 1=vector only)
# CHUNK_SIZE=256
# CHUNK_OVERLAP=32
