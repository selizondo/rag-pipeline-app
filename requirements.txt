# Core RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2

# API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# UI
streamlit>=1.35.0
requests>=2.32.0

# LLM backends (at least one required)
anthropic>=0.28.0        # for LLM_PROVIDER=anthropic
# requests already covers Ollama (plain HTTP)

# Utilities
python-dotenv>=1.0.0
